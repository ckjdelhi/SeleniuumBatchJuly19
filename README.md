# SeleniuumBatchJuly19
In this project, we have worked on selenium + TestNG + Maven
Now, we are working on GIT project to setup GitHUB
Below are the commands that we should perform to setup account

download git for window
create project using eclipse
go to folder

git init (only one time to perform)
git add .
git status
git commit -m"Project Added"

//account setup
git config --global user.email "chandan.krk@gmail.com"
git config --global user.name "ckjdelhi"

//send to server
git remote add origin https://github.com/ckjdelhi/MyProject.git
git push -u origin master
